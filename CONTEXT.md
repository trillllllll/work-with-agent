# Work With Agent 领域术语

Work With Agent 是以个人清单为入口、逐步加入 AI 接入和项目记忆的工作工具。以下术语统一表达用户的工作对象及其关系。

## 清单与任务

**任务（Task）**：用户需要处理的一件事，可以先只有标题，随后补充说明、日期和结果。子任务是属于另一任务的可独立完成的工作项。

**清单（List）**：用户组织任务的容器，例如生活事项、学习安排或一个项目的待办集合。
_避免混用_：筛选视图。

**收集箱（Inbox）**：暂未归入任何清单的任务集合，允许用户先记录、后整理。

**视图（View）**：按日期、状态或标签等条件展示任务的方式；同一任务可以出现在多个视图中。
_避免混用_：任务的所属清单。

**项目（Project）**：围绕一项长期工作持续积累任务、目标、材料和经验的工作容器，可以从一份普通清单逐步形成。
_避免混用_：AI 会话文件夹。

**主题（Topic）**：第一版对工作容器的称呼，允许从尚未明确目标的探索开始；其组织任务的作用对应清单，长期工作含义对应项目。

## AI、记忆与接力

**AI 接入**：让外部 AI 工具在用户授权范围内读取和操作本产品中的工作对象。
_避免混用_：内置聊天、任务派发。

**材料（Material）**：任务或项目引用的原始信息，例如文件、链接、笔记和会话记录。
_避免混用_：已确认的项目记忆。

**项目记忆（Memory）**：从项目经历和材料中形成的、可独立理解且具有来源的事实、决策、约束或经验。
_避免混用_：全部聊天历史、当前任务列表。

**接力（Handoff）**：围绕一项任务向执行者交接工作并接收结果的一次过程；同一任务可以发生多次接力。
_避免混用_：共享数据访问、接力摘要。

**执行（Run）**：执行者对一项工作的一次尝试，具有自己的进展与结果；同一任务可以经历多次执行。
_避免混用_：用户任务、任务已经完成。

**验收**：用户检查执行结果，决定是否采纳以及任务是否完成。
_避免混用_：收到回复、执行结束。

**成果（Artifact）**：任务执行产生的可复用输出，例如报告、设计文件、代码提交或结果链接。
